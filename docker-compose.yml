version: "3"
services:
  node-app:
    build:
      context: ./node-app/
    volumes:
      - ./node-app:/root
    tty: true
    stdin_open: true

  browser-app:
    build:
      context: ./browser-app/
    volumes:
      - ./browser-app:/root
    tty: true
    stdin_open: true
